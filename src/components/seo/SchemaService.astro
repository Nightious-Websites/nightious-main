---
interface Props {
  title: string
  description: string
  slug: string
  category?: string
}

const { title, description, slug, category } = Astro.props

const schema = {
  "@context": "https://schema.org",
  "@type": "Service",
  "@id": `https://nightious.com/services/${slug}/#service`,
  "name": title,
  "description": description,
  "url": `https://nightious.com/services/${slug}`,
  "provider": {
    "@type": "Organization",
    "@id": "https://nightious.com/#organization",
    "name": "Nightious",
  },
  "areaServed": "Worldwide",
  "serviceType": category ?? "Technology Services",
}
---

<script type="application/ld+json" set:html={JSON.stringify(schema)} />
